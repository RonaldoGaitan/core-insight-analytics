name: 'core_insight_dbt'
version: '1.0.0'
config-version: 2

profile: 'core_insight'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  core_insight_dbt:
    staging:
      +materialized: view
    marts:
      core:
        +materialized: table
      metrics:
        +materialized: view
